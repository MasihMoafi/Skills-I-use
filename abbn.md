# User Abbreviations

Use these as context hints, not hard rewrites; normal English meanings still apply when ambiguous.

```text
fmy = familiarize urself with the project
de = the
an = and
check = create a checklist, only when context clearly means checklist; in terminal, `check` is the proxy-env alias
dz = these
ctu = continue
urs = yourself
mys = myself
vt = with
4x = for example
+ = add/ furthermore 
-> = because
<</ >> = relatively, 
sh/tin = small, short, tiny
feel free to = ff2
** / imp = important
ver = verify/ verification
bc = background
fr = from
flg = following
desc = description
wr = write 
summ = summary
term = terminal
eff = efficient/efficiency/effectively
nec, unnec = necessary, unnecessary
f = file
l = learn/learning
ins = instead
2 = to 
ass = at session start
Uppercase B, C = Be, See,
tr = True
```
